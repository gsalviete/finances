// Config fina: as regras vivem em packages/config/eslint (fonte única).
// O flat config da raiz é resolvido por todos os workspaces.
import config from '@finances/config/eslint/index.mjs';

export default config;
