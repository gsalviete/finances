// Placeholder da Fase 1: garante que o runner está configurado.
import * as ui from '../src';

describe('@finances/ui (stub)', () => {
  it('carrega o módulo sem erros', () => {
    expect(ui).toBeDefined();
  });
});
