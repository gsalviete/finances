// Placeholder da Fase 1: garante que o runner está configurado.
// Testes reais chegam com Money/Time (Fase 5) e schemas Zod (Fase 6).
import * as shared from '../src';

describe('@finances/shared (stub)', () => {
  it('carrega o módulo sem erros', () => {
    expect(shared).toBeDefined();
  });
});
