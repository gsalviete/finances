{
  "name": "@finances/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Fonte única: schemas Zod, tipos, Money, Time (conteúdo nas Fases 5–6)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "TZ=UTC jest",
    "test:coverage": "TZ=UTC jest --coverage"
  },
  "devDependencies": {
    "@finances/config": "workspace:*",
    "@types/jest": "^29.0.0",
    "@types/node": "^24.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
