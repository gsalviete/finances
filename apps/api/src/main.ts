// Stub da Fase 1. O bootstrap real (NestJS, Pino, Swagger, filtro de erros)
// é entregue na Fase 7 do IMPLEMENTATION_ROADMAP.md.
export const APP_NAME = 'finances-api';

function main(): void {
  console.log(`[${APP_NAME}] stub da Fase 1 — API real a partir da Fase 7.`);
}

// Executa apenas quando invocado diretamente (não ao ser importado em testes).
if (require.main === module) {
  main();
}
