// Stub da Fase 1. O bootstrap real (Next.js App Router, tokens, temas)
// é entregue na Fase 19 do IMPLEMENTATION_ROADMAP.md.
export const APP_NAME = 'finances-web';

function main(): void {
  console.log(`[${APP_NAME}] stub da Fase 1 — frontend real a partir da Fase 19.`);
}

// Executa apenas quando invocado diretamente (não ao ser importado em testes).
if (require.main === module) {
  main();
}
